# Reasoning Extensions

Reasoning contracts remain available in `@astrospec/reasoning` for advanced interoperability use cases.

They are now positioned as a secondary extension lane rather than the homepage identity of the repo.
