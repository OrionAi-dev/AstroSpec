# @openspec/schema

Canonical OpenSpec JSON Schema bundle plus schema-loading helpers.

This package is the standards-facing schema entrypoint and ships only canonical OpenSpec schemas. `@mcp-secure-context/core` owns Secure Context container schemas separately.
