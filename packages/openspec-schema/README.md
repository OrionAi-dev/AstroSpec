# @openspec/schema

Canonical OpenSpec JSON Schema bundle plus schema-loading helpers.

This package is the standards-facing schema entrypoint. `@mcp-secure-context/core` remains the umbrella framework package and reuses the same bundled schema assets.
