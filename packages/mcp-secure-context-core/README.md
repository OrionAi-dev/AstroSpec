# @mcp-secure-context/core

Core types and schema access for MCP Secure Context Sharing.

This package defines:

- portable `ContextContainer` envelopes
- v0.1 payload types for `UserContext`, `TaskState`, `KnowledgeObject`, and `MemoryReference`
- policy, provenance, and verification metadata types
- bundled JSON Schemas for the container surface
