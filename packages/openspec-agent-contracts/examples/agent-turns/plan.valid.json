{
  "schemaVersion": "1.0",
  "intent": "plan",
  "objective": "Implement orchestrator runtime",
  "constraints": ["read-only default", "write approval required"],
  "assumptions": ["retrieval service available"],
  "steps": [
    {
      "id": "step-1",
      "title": "Gather context",
      "description": "Collect memory and repo context",
      "toolIntents": [{ "name": "filesystem.list", "mode": "read" }],
      "acceptanceCheck": "Context bundle created"
    }
  ],
  "riskRollback": ["pause on write"],
  "completionChecks": ["plan validated"]
}
